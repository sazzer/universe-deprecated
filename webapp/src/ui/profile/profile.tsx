import { Loader } from "../components/loader";
import React from "react";

/**
 * Area of the Profile Page to manage the User Profile
 */
export const UserProfileArea: React.FC = () => {
  return <Loader loading={true}>Hello</Loader>;
};
